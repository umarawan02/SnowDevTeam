# dependencies
node_modules/
.pnpm-store/

# env / secrets
.env
.env.local
apps/web/.env
apps/web/.env.local

# next.js
.next/
apps/web/.next/
out/

# prisma / sqlite
**/dev.db
**/dev.db-journal
**/*.db
**/*.db-journal

# build output
dist/
build/
# ...but not source route folders that happen to be named "build"
!apps/web/src/**/build/

# now-sdk: stored instance credentials + build/pack output
**/.now-sdk/
servicenow/**/dist/
servicenow/**/target/
servicenow/**/.now/

# Customer Fluent projects (REFACTOR_BRIEF Phase 1) — each is its own git
# repo with its own node_modules; never committed here.
/workspaces/

# now-sdk: per-deploy generated app source (written from the DB artifact on Approve).
# keys.ts stays tracked as the record-identity source of truth.
servicenow/delivery-app/src/fluent/**
!servicenow/delivery-app/src/fluent/generated/
!servicenow/delivery-app/src/fluent/generated/**
servicenow/delivery-app/src/server/**
!servicenow/delivery-app/src/server/tsconfig.json

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
