# ServiceNow PDI smoke findings — dev424712

> Generated by `pnpm --filter web smoke cmtoeynmq0002zbq4ggke77pf` on 2026-09-05T22:46:21.781Z.
> Instance: https://dev424712.service-now.com · release: australia · authMode: oauth_cc

These answer the "Open items" in NATIVE_ENGINE_BRIEF.md and gate the design of phases 4–6.

## Summary

| Open item | Verdict | Finding |
|---|---|---|
| open item #1 basic | **FAIL** | concoursepicker PUT did not update apps.current_app for this session |
| open item #1 OAuth | **FAIL** | concoursepicker PUT did not update apps.current_app for this session |
| open item #2 | **FAIL** | preference "sys_update_set" read back OK but the write leaked to the Default set |
| open item #3 | **PASS** | sn_cicd update_set/create works (sync here) — it did NOT set the created set current |
| open item #4 | **PASS** | OAuth client has unrestricted access to Global + scoped reads/writes |
| open item #5 | **INCONCLUSIVE** | collected evidence; the definitive create-then-delete test is deferred to a hands-on run |
| open item #6 | **PASS** | ATF is active and a client test runner appears to be scheduled — client-side steps should run |
| open item #7 | **INCONCLUSIVE** | sysparm_transaction_scope RE-SCOPED the write to "x_1460392_delivery" — this CONTRADICTS the brief ("Do not use / expected: none") |

---

## smoke-scope-write.mts

```
◇ injected env (0) from .env // tip: ⌘ enable debugging { debug: true }
◇ injected env (0) from ..\..\.env // tip: ◈ encrypted .env [www.dotenvx.com]

=== SMOKE: scope-write / basic  (open item #1 (basic)) ===
verdict : FAIL
finding : concoursepicker PUT did not update apps.current_app for this session
detail  : current application is "global", expected "f53ca6b11fdb4e81af25056bdf0f44ea" — refusing to write
action  : server-side scripted resource required

=== SMOKE: scope-write / oauth_cc  (open item #1 (OAuth)) ===
verdict : FAIL
finding : concoursepicker PUT did not update apps.current_app for this session
detail  : current application is "global", expected "f53ca6b11fdb4e81af25056bdf0f44ea" — refusing to write
action  : server-side scripted resource required
```

## smoke-updateset.mts

```
◇ injected env (0) from .env // tip: ⌁ auth for agents [www.vestauth.com]
◇ injected env (0) from ..\..\.env // tip: ◈ secrets for agents [www.dotenvx.com]

=== SMOKE: updateset / current-set preference  (open item #2) ===
verdict : FAIL
finding : preference "sys_update_set" read back OK but the write leaked to the Default set
detail  : honoured pref: sys_update_set; captured in our set: false; leaked to Default: true
action  : a plain preference write is not enough — need a scripted `GlideUpdateSet.setCurrent` REST resource on the instance

=== SMOKE: updateset / sn_cicd create  (open item #3) ===
verdict : PASS
finding : sn_cicd update_set/create works (sync here) — it did NOT set the created set current
detail  : params on query string; update_set_id=b208f8570fc74b501cd358c300d1b2b7; sets-current=false
action  : the engine must set the created set current itself (a server-side scripted resource, since a plain preference write is not honoured — see #2)
```

## smoke-scope-restriction.mts

```
◇ injected env (0) from .env // tip: ⌁ auth for agents [www.vestauth.com]
◇ injected env (0) from ..\..\.env // tip: ◈ secrets for agents [www.dotenvx.com]

=== SMOKE: scope-restriction  (open item #4) ===
verdict : PASS
finding : OAuth client has unrestricted access to Global + scoped reads/writes
detail  : release=australia (Zurich+); unscoped read 200, unscoped write 201, cross-scope read 200
action  : no action — engine client is correctly scoped
```

## smoke-dictionary.mts

```
◇ injected env (0) from .env // tip: ⌘ custom filepath { path: '/custom/path/.env' }
◇ injected env (0) from ..\..\.env // tip: ⌘ custom filepath { path: '/custom/path/.env' }

=== SMOKE: dictionary write viability (detect-only)  (open item #5) ===
verdict : INCONCLUSIVE
finding : collected evidence; the definitive create-then-delete test is deferred to a hands-on run
detail  : create/write ACLs on sys_dictionary: sys_dictionary:create(active=true), sys_dictionary.*:write(active=true), sys_dictionary:write(active=true), sys_dictionary:create(active=true), sys_dictionary.*:write(active=true), sys_dictionary:write(active=true), sys_dictionary.*:write(active=true), sys_dictionary:write(active=true), sys_dictionary.*:write(active=true), sys_dictionary:create(active=true), sys_dictionary:write(active=true)
        relevant properties: glide.ais.ingestion.dictionary_term_batcher_queue_size=1000, glide.spell.dictionary.en=en.dic
        recent u_ fields (created via UI/API): u_guest_wifi_voucher.u_business_justification by admin; u_guest_wifi_voucher.u_access_duration_days by admin; u_guest_wifi_voucher.u_voucher_code by admin; u_guest_wifi_voucher.u_visitor_email by admin; u_guest_wifi_voucher.u_expiry by admin; u_guest_wifi_voucher.u_requester by admin; u_guest_wifi_voucher.u_visitor_name by admin; u_guest_wifi_voucher.u_issued_on by admin
action  : Phase 4 schema route: prefer Fluent tier or human for table/field creation. If Table API turns out viable, add sys_dictionary (fields on existing tables only) to the allow-list flagged high-risk.
```

## smoke-atf.mts

```
◇ injected env (0) from .env // tip: ⌁ auth for agents [www.vestauth.com]
◇ injected env (0) from ..\..\.env // tip: ⌘ enable debugging { debug: true }

=== SMOKE: ATF headless capability (detect-only)  (open item #6) ===
verdict : PASS
finding : ATF is active and a client test runner appears to be scheduled — client-side steps should run
detail  : sys_atf_test readable: true; runner.enabled property: unset; scheduled jobs matching ATF/client-test: @dotwalk/atf-commons-server/SNBOQProcess, UA Platform Analytics Timed Uploader, ATF test runner reaper, ATFPerformancePauseReaper, ATF Scheduler, Clean Unused ATF Step Snapshots, ATF Log Cleanup, events process platform_analytics, UA Platform Analytics Timed Uploader, events process platform_analytics_export
action  : Phase 5 §5.5: run server-side ATF via sn_cicd/testsuite/run; document that a scheduled client test runner (or a container runner) must be configured for UI-interaction steps.
```

## smoke-transaction-scope.mts

```
◇ injected env (0) from .env // tip: ⌘ enable debugging { debug: true }
◇ injected env (0) from ..\..\.env // tip: ⌘ override existing { override: true }

=== SMOKE: sysparm_transaction_scope effect  (open item #7) ===
verdict : INCONCLUSIVE
finding : sysparm_transaction_scope RE-SCOPED the write to "x_1460392_delivery" — this CONTRADICTS the brief ("Do not use / expected: none")
detail  : sent sysparm_transaction_scope=x_1460392_delivery while Global was current; record landed in scope "x_1460392_delivery"
action  : DECISION NEEDED: sysparm_transaction_scope works on this release and may be a simpler scope-control mechanism than a server-side scripted resource — but it is undocumented and the brief forbids it. Raise with the user before Phase 4.
```
