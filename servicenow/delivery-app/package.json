{
    "name": "sn-delivery-app",
    "version": "0.0.1",
    "description": "ServiceNow Fluent app — deploy target for the AI delivery pipeline",
    "license": "UNLICENSED",
    "private": true,
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies"
    },
    "devDependencies": {
        "@servicenow/sdk": "4.11.2",
        "@servicenow/glide": "27.0.5",
        "typescript": "5.5.4"
    }
}